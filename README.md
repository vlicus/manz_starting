# .gitignore contains package-lock.json and node_modules

# !!!!!! IMPORTANT !!!!!!
# in node_modules (standard eslint)
# package.json exports stardard eslint configuration with the use of:
# "eslintConfig": {
#   "extends": "./node_modules/standard/eslintrc.json"
#  }
